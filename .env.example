# FastAPI backend base URL (see ../backend)
NEXT_PUBLIC_API_URL=http://localhost:8000

# web_worker WebSocket base URL, room-scoped at /ws/{stack_box_id} (see ../web_worker)
NEXT_PUBLIC_WORKER_URL=ws://localhost:3000
